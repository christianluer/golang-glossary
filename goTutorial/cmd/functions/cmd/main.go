package main

func main() {
	// func (r receiver) identifier(p parameter(s)) (return(s)) {code}(arguments(s)) any part can appear or not, and the func is treated as any type in golang ex int
	//////////////////////////////////////////
	// anonymous.RunAnonymousFunc()
	// callback.Execute()
	// recursion.CallFactorial(5)
}
